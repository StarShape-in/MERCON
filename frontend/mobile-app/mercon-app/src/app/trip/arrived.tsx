/** Route: /trip/arrived — driver confirms arrival at the delivery location. */
import DestinationReachedScreen from '@/screens/driver/DestinationReachedScreen';

export default function ArrivedRoute() {
  return <DestinationReachedScreen />;
}
