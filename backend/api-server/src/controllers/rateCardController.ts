/**
 * @deprecated Legacy compatibility wrapper. Use `quotationController.ts` instead.
 * TODO: Remove when legacy rate-cards references are fully deprecated.
 */
export * from './quotationController';
